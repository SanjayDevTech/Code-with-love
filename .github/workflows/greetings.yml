name: Greetings

on: pull_request_target

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.SECRETS }}
        pr-message: 'Congratualtions on making your first Pull Request'' first pr'
